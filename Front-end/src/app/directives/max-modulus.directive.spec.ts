import { MaxModulusDirective } from './max-modulus.directive';

describe('MaxModulusDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxModulusDirective();
    expect(directive).toBeTruthy();
  });
});
