import { MinSideLengthDirective } from './min-side-length.directive';

describe('MinSideLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new MinSideLengthDirective();
    expect(directive).toBeTruthy();
  });
});
