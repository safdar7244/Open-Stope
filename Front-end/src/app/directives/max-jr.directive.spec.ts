import { MaxJrDirective } from './max-jr.directive';

describe('MaxJrDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxJrDirective();
    expect(directive).toBeTruthy();
  });
});
