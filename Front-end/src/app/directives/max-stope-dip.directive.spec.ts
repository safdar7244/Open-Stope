import { MaxStopeDipDirective } from './max-stope-dip.directive';

describe('MaxStopeDipDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxStopeDipDirective();
    expect(directive).toBeTruthy();
  });
});
