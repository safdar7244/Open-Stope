import { MinFhWidthDirective } from './min-fh-width.directive';

describe('MinFhWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MinFhWidthDirective();
    expect(directive).toBeTruthy();
  });
});
