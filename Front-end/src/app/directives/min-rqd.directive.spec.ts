import { MinRqdDirective } from './min-rqd.directive';

describe('MinRqdDirective', () => {
  it('should create an instance', () => {
    const directive = new MinRqdDirective();
    expect(directive).toBeTruthy();
  });
});
