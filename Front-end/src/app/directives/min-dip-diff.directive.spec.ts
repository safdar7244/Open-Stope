import { MinDipDiffDirective } from './min-dip-diff.directive';

describe('MinDipDiffDirective', () => {
  it('should create an instance', () => {
    const directive = new MinDipDiffDirective();
    expect(directive).toBeTruthy();
  });
});
