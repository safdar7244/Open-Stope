import { MaxRqdDirective } from './max-rqd.directive';

describe('MaxRqdDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxRqdDirective();
    expect(directive).toBeTruthy();
  });
});
