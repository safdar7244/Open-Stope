import { MaxSideWidthDirective } from './max-side-width.directive';

describe('MaxSideWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxSideWidthDirective();
    expect(directive).toBeTruthy();
  });
});
