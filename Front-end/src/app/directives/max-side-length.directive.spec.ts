import { MaxSideLengthDirective } from './max-side-length.directive';

describe('MaxSideLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxSideLengthDirective();
    expect(directive).toBeTruthy();
  });
});
