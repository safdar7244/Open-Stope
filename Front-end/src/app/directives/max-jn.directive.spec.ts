import { MaxJnDirective } from './max-jn.directive';

describe('MaxJnDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxJnDirective();
    expect(directive).toBeTruthy();
  });
});
