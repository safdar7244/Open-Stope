import { MinJrDirective } from './min-jr.directive';

describe('MinJrDirective', () => {
  it('should create an instance', () => {
    const directive = new MinJrDirective();
    expect(directive).toBeTruthy();
  });
});
