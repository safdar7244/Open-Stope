import { MinUcsDirective } from './min-ucs.directive';

describe('MinUcsDirective', () => {
  it('should create an instance', () => {
    const directive = new MinUcsDirective();
    expect(directive).toBeTruthy();
  });
});
