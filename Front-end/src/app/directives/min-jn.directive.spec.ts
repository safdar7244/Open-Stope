import { MinJnDirective } from './min-jn.directive';

describe('MinJnDirective', () => {
  it('should create an instance', () => {
    const directive = new MinJnDirective();
    expect(directive).toBeTruthy();
  });
});
