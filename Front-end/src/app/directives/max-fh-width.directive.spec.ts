import { MaxFhWidthDirective } from './max-fh-width.directive';

describe('MaxFhWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxFhWidthDirective();
    expect(directive).toBeTruthy();
  });
});
