import { MinHeightDirective } from './min-height.directive';

describe('MinHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new MinHeightDirective();
    expect(directive).toBeTruthy();
  });
});
