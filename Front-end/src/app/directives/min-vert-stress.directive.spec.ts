import { MinVertStressDirective } from './min-vert-stress.directive';

describe('MinVertStressDirective', () => {
  it('should create an instance', () => {
    const directive = new MinVertStressDirective();
    expect(directive).toBeTruthy();
  });
});
