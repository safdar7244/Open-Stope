import { MinStopeDipDirective } from './min-stope-dip.directive';

describe('MinStopeDipDirective', () => {
  it('should create an instance', () => {
    const directive = new MinStopeDipDirective();
    expect(directive).toBeTruthy();
  });
});
