import { MaxDipDiffDirective } from './max-dip-diff.directive';

describe('MaxDipDiffDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxDipDiffDirective();
    expect(directive).toBeTruthy();
  });
});
