import { MinSideBottomWidthDirective } from './min-side-bottom-width.directive';

describe('MinSideBottomWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MinSideBottomWidthDirective();
    expect(directive).toBeTruthy();
  });
});
