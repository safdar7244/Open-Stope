import { MinDepthDirective } from './min-depth.directive';

describe('MinDepthDirective', () => {
  it('should create an instance', () => {
    const directive = new MinDepthDirective();
    expect(directive).toBeTruthy();
  });
});
