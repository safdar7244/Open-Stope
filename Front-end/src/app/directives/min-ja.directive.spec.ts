import { MinJaDirective } from './min-ja.directive';

describe('MinJaDirective', () => {
  it('should create an instance', () => {
    const directive = new MinJaDirective();
    expect(directive).toBeTruthy();
  });
});
