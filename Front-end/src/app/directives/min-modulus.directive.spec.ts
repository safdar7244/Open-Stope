import { MinModulusDirective } from './min-modulus.directive';

describe('MinModulusDirective', () => {
  it('should create an instance', () => {
    const directive = new MinModulusDirective();
    expect(directive).toBeTruthy();
  });
});
