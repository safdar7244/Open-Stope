import { MinSideTopWidthDirective } from './min-side-top-width.directive';

describe('MinSideTopWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MinSideTopWidthDirective();
    expect(directive).toBeTruthy();
  });
});
