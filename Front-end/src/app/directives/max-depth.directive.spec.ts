import { MaxDepthDirective } from './max-depth.directive';

describe('MaxDepthDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxDepthDirective();
    expect(directive).toBeTruthy();
  });
});
