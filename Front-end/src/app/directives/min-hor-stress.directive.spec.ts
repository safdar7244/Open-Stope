import { MinHorStressDirective } from './min-hor-stress.directive';

describe('MinHorStressDirective', () => {
  it('should create an instance', () => {
    const directive = new MinHorStressDirective();
    expect(directive).toBeTruthy();
  });
});
