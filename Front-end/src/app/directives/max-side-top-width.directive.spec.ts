import { MaxSideTopWidthDirective } from './max-side-top-width.directive';

describe('MaxSideTopWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxSideTopWidthDirective();
    expect(directive).toBeTruthy();
  });
});
