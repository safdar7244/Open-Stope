import { MaxUcsDirective } from './max-ucs.directive';

describe('MaxUcsDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxUcsDirective();
    expect(directive).toBeTruthy();
  });
});
