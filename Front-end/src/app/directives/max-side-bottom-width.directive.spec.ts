import { MaxSideBottomWidthDirective } from './max-side-bottom-width.directive';

describe('MaxSideBottomWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxSideBottomWidthDirective();
    expect(directive).toBeTruthy();
  });
});
