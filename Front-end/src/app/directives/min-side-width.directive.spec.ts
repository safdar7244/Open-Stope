import { MinSideWidthDirective } from './min-side-width.directive';

describe('MinSideWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new MinSideWidthDirective();
    expect(directive).toBeTruthy();
  });
});
