import { MaxJaDirective } from './max-ja.directive';

describe('MaxJaDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxJaDirective();
    expect(directive).toBeTruthy();
  });
});
